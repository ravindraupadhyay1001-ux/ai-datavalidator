fastapi
uvicorn[standard]
bcrypt
PyJWT[crypto]>=2.8
cryptography>=41
pandas>=2.2
numpy
openpyxl
pyarrow
python-multipart
pydantic
boto3

chardet
jinja2
python-dotenv

langchain-core>=1.2.21
langchain-aws>=1.4.1

# Multi-provider LLM fallback (Groq + OpenRouter use the OpenAI-compatible client too)
openai>=1.30
google-genai>=1.0            # current Gemini SDK
google-generativeai>=0.7     # deprecated Gemini SDK -- kept as a fallback for now
anthropic>=0.40

pdfplumber
fastavro

pywin32; sys_platform == "win32" # Outlook email via win32com (Windows only)
apscheduler>=3.10
paramiko                         # SFTP (Unix/Linux servers)
httpx                            # REST API
pyodbc                           # Microsoft SQL Server (requires ODBC Driver 17 on host)
psycopg2-binary                  # PostgreSQL
pymysql                          # MySQL / MariaDB
snowflake-connector-python       # Snowflake
azure-storage-blob               # Azure Blob Storage
google-cloud-storage             # Google Cloud Storage
kafka-python                     # Kafka
simple-salesforce                # Salesforce
databricks-sql-connector         # Databricks SQL warehouses
teradatasql                      # Teradata
google-cloud-bigquery            # Google BigQuery
pymongo                          # MongoDB
# Redshift reuses psycopg2-binary above -- wire-compatible with Postgres
ldap3                            # LDAP / Active Directory auth (SSO)
authlib                          # OIDC SSO (Authorization Code flow)
itsdangerous                     # required by Starlette's SessionMiddleware (OIDC state)
# SharePoint uses the Microsoft Graph REST API via httpx -- no extra SDK needed
# Optional (install manually if needed):
# cx_Oracle                      # Oracle DB
# oracledb                       # Oracle DB (newer driver)
